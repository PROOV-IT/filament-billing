# Billing Dashboard

The plugin ships with dashboard widgets instead of a custom overview Blade.

The default widget set includes:

- a billing metrics widget
- a recent invoices table widget
- a recent quotes table widget

These widgets are registered on the panel dashboard when `filament-billing.dashboard.enabled` is `true`.
You can still build your own dashboard pages on top of the same billing resources if you want a different layout.
