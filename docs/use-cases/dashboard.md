# Billing Dashboard

The initial version of the plugin ships with a single overview page.

It shows:

- company count
- customer count
- invoice count
- quote count
- payment count
- recent invoices

This gives a Filament panel a first useful entry point into the billing core while keeping the package easy to extend later.
